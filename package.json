{
  "name": "copy-of-t-cardio",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm install --legacy-peer-deps && vite build && cd server && npm install --legacy-peer-deps && npm run init-db",
    "build:full": "npm install --legacy-peer-deps && cd server && npm install --legacy-peer-deps && cd .. && npm run build && cd server && npm run init-db",
    "start:production": "cd server && npm start",
    "preview": "vite preview",
    "build:netlify": "npm run build",
    "start": "node server/server-production.js",
    "deploy:netlify": "node deploy-netlify.cjs",
    "deploy:railway": "railway up",
    "deploy:render": "npm run build:full"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "@google/genai": "^1.20.0",
    "recharts": "^2.12.7",
    "dexie": "^4.0.8",
    "clsx": "^2.1.1",
    "tailwind-merge": "^2.4.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}
